import React from 'react';
import logo from '../../assets/logo.svg';

const Logo = () => (
  <div className="logo">
    <img src={logo} alt="logotype" width="200px" />
  </div>
);

export default Logo;
